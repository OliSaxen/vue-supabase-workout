[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"  # Optional, for serverless functions

[build.environment]
  NODE_VERSION = "22"  # Use Node.js

# For Vue Router history mode - IMPORTANT!
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200